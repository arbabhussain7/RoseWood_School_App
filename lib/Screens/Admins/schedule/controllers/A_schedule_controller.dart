import 'package:flutter/material.dart';

class AScheduleController extends StatelessWidget {
  const AScheduleController({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
